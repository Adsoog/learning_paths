package main

import "fmt"
func main() {

	const premiumPlanName = "Premiun Plan"
	const basicPlanName = "Basic Plan"	
	// Constans cants use the shor way declaration const premiumPlanName := and they can be strings, booleans or numeric values 

	fmt.Println("plan: ", premiumPlanName)
	fmt.Println("plan: ", basicPlanName)
}